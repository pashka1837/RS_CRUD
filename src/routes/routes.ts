// import { response } from "node:http";

const routesAr = [
  [
    "/api/users",
    {
      GET: (_req, res) => {
        // response(res, { data: { message: "running nodejs api" } });
      },
    },
  ],
];
